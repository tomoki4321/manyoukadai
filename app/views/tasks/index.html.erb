<h1>タスク一覧</h1>
<p><%=notice%></p>
<table>
  <tr>
    <th>タスク名</th>
    <th>内容</th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.task_name %></td>
    <td><%= task.task_content %></td>
  </tr>
<% end %>
</table>
<%=link_to "タスク作成",new_task_path%>